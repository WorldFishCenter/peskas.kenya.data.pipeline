# Core ingestion logic for price survey data

Core ingestion logic for price survey data

## Usage

``` r
ingest_price_survey_version(version, kobo_config, storage_config)
```

## Arguments

- version:

  Version identifier (e.g., "v1", "v2")

- kobo_config:

  Configuration object containing Kobo connection details

- storage_config:

  Configuration object containing storage details

## Value

No return value. Processes and uploads data.
