% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing-surveys.R
\name{preprocess_abalobi_activities}
\alias{preprocess_abalobi_activities}
\title{Preprocess ABALOBI Monitor Activities}
\usage{
preprocess_abalobi_activities(log_threshold = logger::DEBUG)
}
\arguments{
\item{log_threshold}{Logging threshold level (default: logger::DEBUG)}
}
\value{
No return value. Function processes the data and uploads the result
as a Parquet file to Google Cloud Storage.
}
\description{
This function preprocesses raw ABALOBI Monitor activity submission data from
Google Cloud Storage. It extracts trip-level information, reshapes the nested
catch list to a long (one row per species) format, standardises field names
and types, and uploads the result as a Parquet file to Google Cloud Storage.
}
\details{
The function performs the following main operations:
\enumerate{
\item \strong{Downloads raw data}: Retrieves the flattened raw ABALOBI activities
from Google Cloud Storage.
\item \strong{Resolves localised names}: Collapses the multilingual name maps for the
monitoring site, fishing site, vessel type and propulsion into single
English-preferred strings using \code{\link[=coalesce_abalobi_localised]{coalesce_abalobi_localised()}}.
\item \strong{Extracts trip information}: Selects and renames relevant trip-level
fields (one row per activity submission), including:
\itemize{
\item Monitor and fisher identity
\item Landing details (date, site, coordinates, region and FAO area)
\item Vessel details (type, name, registration, propulsion, crew)
\item Trip details (start/end times, duration, group harvesting)
\item Catch outcome and basket weight
}
\item \strong{Reshapes catch data}: Transforms the nested \code{catchList} from wide to
long format using \code{\link[=reshape_abalobi_catch]{reshape_abalobi_catch()}}.
\item \strong{Type conversions and calculations}: Parses timestamps, derives trip
duration in hours, and coerces numeric and logical fields.
\item \strong{Joins trip and catch data}: Combines trip information with catch
records using a full join on \code{submission_id}.
\item \strong{Uploads processed data}: Saves the preprocessed data as a Parquet file
to Google Cloud Storage.
}

The per-individual biological samples are summarised at the species level
within the reshaped catch (\code{n_samples}, \code{sampled_count}); the full sample
detail can be obtained separately with \code{\link[=reshape_abalobi_samples]{reshape_abalobi_samples()}}.
}
\section{Pipeline Integration}{

This function is part of the ABALOBI data pipeline sequence:
\enumerate{
\item \code{ingest_abalobi_activities()} - Downloads raw data from the ABALOBI API
\item \strong{\code{preprocess_abalobi_activities()}} - Cleans and standardises data (this function)
\item Validation step (to be implemented)
\item Export step (to be implemented)
}
}

\examples{
\dontrun{
preprocess_abalobi_activities()

# Run with custom logging level
preprocess_abalobi_activities(log_threshold = logger::INFO)
}
}
\keyword{preprocessing}
\keyword{workflow}
