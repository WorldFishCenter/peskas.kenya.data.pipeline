% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingestion.R
\name{get_abalobi_data}
\alias{get_abalobi_data}
\title{Retrieve activity submission data from the ABALOBI API}
\usage{
get_abalobi_data(
  base_url,
  api_key,
  organisation_ids,
  start_date,
  end_date,
  languages = NULL,
  page_limit = 100
)
}
\arguments{
\item{base_url}{Character string. Base URL of the ABALOBI Monitor data API
(e.g. \code{"https://api.abalobi.org/monitor/data/1"}).}

\item{api_key}{Character string. API key sent in the custom \code{Authenticate}
header.}

\item{organisation_ids}{Character vector of organisation IDs (tenant codes)
for which data is returned. Sent as a comma-separated \code{organisationIds}
query parameter.}

\item{start_date}{Character string. Start of the date range (inclusive),
based on \code{loggedDateTimeUTC}, in ISO 8601 format.}

\item{end_date}{Character string. End of the date range (inclusive), based on
\code{loggedDateTimeUTC}, in ISO 8601 format.}

\item{languages}{Character vector of language/locale codes to include in
localised name objects (e.g. \code{c("ke-en", "ke-sw")}). If \code{NULL}, all
available language codes are returned.}

\item{page_limit}{Integer. Maximum number of records requested per page
(maps to the API \code{limit} parameter). Defaults to 100.}
}
\value{
A list of activity submission records (each itself a nested list), as
returned by the API and ready to be flattened with
\code{\link[=flatten_abalobi_record]{flatten_abalobi_record()}}.
}
\description{
Low-level helper that queries the ABALOBI Monitor System Data API
(\code{GET /activities}) and returns all activity submission records for the
requested organisations and date range, transparently handling pagination.
}
\details{
The endpoint returns a paginated array of page objects, each holding a \code{data}
array of records together with \code{totalCount}, \code{offset} and \code{limit}. This
function accumulates the \code{data} records across successive pages, increasing
\code{offset} by \code{page_limit} each time, until every record reported by
\code{totalCount} has been retrieved (or an empty page is returned).
}
\keyword{ingestion}
