% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingestion.R
\name{ingest_abalobi_activities}
\alias{ingest_abalobi_activities}
\title{Download and Process ABALOBI Monitor Activities from the ABALOBI API}
\usage{
ingest_abalobi_activities(
  start_date = NULL,
  end_date = NULL,
  log_threshold = logger::DEBUG
)
}
\arguments{
\item{start_date}{Start of the date range (inclusive), based on
\code{loggedDateTimeUTC}, in ISO 8601 format (e.g. \code{"2024-01-01T00:00:00.000Z"}).
Defaults to \code{NULL}, in which case the value from configuration
(\code{ingestion$abalobi$api$start_date}) is used.}

\item{end_date}{End of the date range (inclusive), based on
\code{loggedDateTimeUTC}, in ISO 8601 format. Defaults to \code{NULL}, in which case
the current UTC time is used, so that each run pulls the full history up to
now.}

\item{log_threshold}{Logging threshold level (default: logger::DEBUG)}
}
\value{
No return value. Function downloads data, processes it, and uploads
to Google Cloud Storage.
}
\description{
This function retrieves activity submission data from the ABALOBI Monitor
System Data API (\code{GET /activities}), flattens the deeply nested JSON payload
into a tabular format, and uploads the raw data as a Parquet file to Google
Cloud Storage.
}
\details{
The function performs the following steps:
\enumerate{
\item Reads configuration settings.
\item Downloads activity submission data from the ABALOBI API using
\code{\link[=get_abalobi_data]{get_abalobi_data()}}, iterating over all pages.
\item Checks for uniqueness of submission UUIDs.
\item Flattens each nested submission into a wide tibble using
\code{\link[=flatten_abalobi_record]{flatten_abalobi_record()}} and renames \code{uuid} to \code{submission_id}.
\item Uploads the raw data as a Parquet file to Google Cloud Storage.
}

The ABALOBI API requires an API key (passed in the \code{Authenticate} header), a
\code{loggedDateTimeUTC} date range and one or more organisation IDs (tenant
codes). All of these are read from \code{conf$ingestion$abalobi$api}.

Note: Preprocessing is handled by \code{\link[=preprocess_abalobi_activities]{preprocess_abalobi_activities()}}.
Validation, matching and export stages for ABALOBI data are not yet
implemented.
}
\examples{
\dontrun{
ingest_abalobi_activities()

# Restrict the pull to a specific date range
ingest_abalobi_activities(
  start_date = "2025-01-01T00:00:00.000Z",
  end_date = "2025-03-31T23:59:59.999Z"
)
}
}
\keyword{ingestion}
\keyword{workflow}
