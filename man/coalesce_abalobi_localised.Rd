% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape-surveys.R
\name{coalesce_abalobi_localised}
\alias{coalesce_abalobi_localised}
\title{Coalesce ABALOBI localised-name columns into a single string}
\usage{
coalesce_abalobi_localised(data, field, prefer = "en")
}
\arguments{
\item{data}{A data frame containing the flattened ABALOBI columns.}

\item{field}{Character string. The shared prefix of the localised-name
columns, without the trailing language code (e.g. \code{"monitoringSite.name"}).}

\item{prefer}{Character string matched (case-insensitively) against the
column names to decide which language takes precedence. Defaults to
\code{"en"}.}
}
\value{
A character vector, one value per row of \code{data}, or all \code{NA} if no
matching column is present.
}
\description{
ABALOBI localised names are stored as a map of language/locale codes to
strings, which flattens to several columns sharing a common prefix (e.g.
\code{monitoringSite.name.ke-en}, \code{monitoringSite.name.ke-sw}). This helper
collapses those columns row-wise into a single character vector, preferring
the language code that matches \code{prefer} (English by default).
}
\keyword{helper}
\keyword{internal}
\keyword{preprocessing}
