% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match-trips.R
\name{compute_survey_matches}
\alias{compute_survey_matches}
\title{Run Matching Pipeline for a Single Survey}
\usage{
compute_survey_matches(survey, conf, registry, all_trips)
}
\arguments{
\item{survey}{Character. Survey identifier: "kefs" or "wcs".}

\item{conf}{List. Configuration object from \code{read_config()}.}

\item{registry}{Data frame. Device registry (pre-loaded by the caller).}

\item{all_trips}{Data frame. GPS trips (pre-loaded by the caller).}
}
\value{
A list with two elements:
\itemize{
\item \strong{records}: Data frame of matched survey-trip pairs, surveys
unmatched to any trip, and any multi-trip-day trip rows produced during
matching. Includes a \code{survey} column identifying the source.
\item \strong{matched_trip_ids}: Character vector of trip IDs that
appeared in the matching output (used by the caller to identify
truly unmatched trips).
}
}
\description{
Loads validated surveys for one survey type, runs fuzzy matching against the
device registry and GPS trips, and returns enriched records plus the set of
trip IDs that were claimed by the match. Called once per survey by
\code{merge_trips()}, which combines results across all surveys.
}
\keyword{internal}
