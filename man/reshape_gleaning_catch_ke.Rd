% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process-catch-gleaning.R
\name{reshape_gleaning_catch_ke}
\alias{reshape_gleaning_catch_ke}
\title{Reshape Kenya Gleaning Catch Data from Wide to Long Format}
\usage{
reshape_gleaning_catch_ke(data = NULL, max_catch = 3)
}
\arguments{
\item{data}{A data frame of the Kenya gleaning export, with raw Kobo repeat
columns named \verb{CATCH.\{i\}.CATCH/...}.}

\item{max_catch}{Maximum number of repeat instances to scan (default 3).}
}
\value{
A long data frame, one row per submission x catch instance x taxon x
size class (plus a context row for submissions without catch detail).
}
\description{
Reshapes the repeated \code{CATCH} group of the Kenya intertidal gleaning
KoboToolbox survey into one tidy long table. Unlike the Zanzibar form, here:
\itemize{
\item \code{CATCH} is a \strong{repeat} group: each submission can hold several
catch instances (\code{CATCH.0}, \code{CATCH.1}, ...), one taxon each.
\item Each instance carries its own \strong{total weight (kg)} and
\strong{price/kg} (no bucket/plastic container method).
\item Size bins are \strong{taxon-specific}: length classes for
gastropods/bivalves/crabs/sea cucumbers, \strong{weight} classes for
octopus, free \strong{text} for \code{others}, and \strong{none} for
seaweed (weight only).
}
}
\details{
Output grain: one row per \code{submission_id} x \code{catch_id} x \code{taxon} x
\code{size_class}. Per-taxon \code{total_weight_kg} and \code{price_per_kg} repeat down the
size-class rows of an instance. Heterogeneous bins are unified via three
columns: \code{size_class} (small/medium/large/xlarge), \code{size_metric}
(\code{length_cm} / \code{weight_kg} / \code{none}), and numeric \code{bound_min}/\code{bound_max}
(cm for length taxa, kg for octopus, NA otherwise). Seaweed yields a single
row (no size class); \code{others} keeps its raw text counts in \code{n_individuals_raw}
alongside the numeric parse in \code{n_individuals}. Submissions with no catch
detail are preserved as a single context row so none is dropped.

Current-form fields are treated as authoritative over legacy duplicates:
gastropod small uses \code{Small_5cm_001} (not \code{small_5}); seaweed weight
coalesces \code{Weight_of_Seaweed_Kg} over the legacy \code{Weight_of_Seaweed_kg}.
Free-text prices/counts are parsed with \code{\link[=parse_numeric_text]{parse_numeric_text()}}, with the
original price kept in \code{price_per_kg_raw}.

The taxon registry below also covers prawns and squid (defined in the form
but unused in this export); they activate automatically if a future export
contains them.
}
\examples{
\dontrun{
catch_long <- reshape_gleaning_catch_ke(catch_info)

# Per-instance catch weight (deduped: weight repeats across size rows)
catch_long |>
  dplyr::distinct(submission_id, catch_id, taxon, total_weight_kg, price_per_kg)
}
}
