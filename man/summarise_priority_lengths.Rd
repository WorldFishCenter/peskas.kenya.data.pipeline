% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reshape-surveys.R
\name{summarise_priority_lengths}
\alias{summarise_priority_lengths}
\title{Collapse individual length measurements to the species they belong to}
\usage{
summarise_priority_lengths(priority_df = NULL)
}
\arguments{
\item{priority_df}{Long priority-species data from \code{\link[=reshape_priority_species]{reshape_priority_species()}}.}
}
\value{
Tibble with one row per \code{submission_id} x \code{priority_species}:
\describe{
\item{submission_id}{Unique identifier for each submission}
\item{priority_species}{Survey label of the measured species}
\item{length_type}{Length convention used (e.g. \code{total_length})}
\item{length_cm}{Mean length of the measured individuals}
\item{length_min_cm, length_max_cm}{Range of the measured individuals}
\item{n_measured}{Number of individuals measured for that species}
\item{measured_weight_kg}{Summed weight of those individuals}
}
}
\description{
\code{PrioritySpeciesCatch} records one row per \emph{measured fish}, while
\code{OverallSampleWeight} records one row per \emph{catch item} (a species in the
weighed sample). The two are nested, and the cross-country API schema carries
a single \code{length_cm} per catch row, so the individuals have to be collapsed
onto their species before the two can be joined.
}
\details{
\code{length_cm} is the plain mean across individuals, which -- because Kenya
records one row per fish rather than per length bin -- is the same
individual-weighted mean the Timor pipeline publishes for the shared API
schema. It is a subsample statistic: \code{measured_weight_kg} is the weight of the
fish actually measured and is generally \emph{less} than the species'
\code{sample_weight}, which covers the whole weighed sample.

Rows carrying no usable length are dropped, so a species measured only with
missing lengths contributes nothing rather than an \code{NaN} mean.
}
\keyword{helper}
\keyword{preprocessing}
